{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        {#        <h1>Hello {{ controller_name }}! ✅</h1>#}
        <h1>Привет {{ app.user.getName }}! ✅</h1>

        <h2>Личные данные</h2>

        {#        {{ dump(controller_name) }}#}
        {#        {{ dump(user) }}#}
        {#        {{ dump(userProfileForm) }}#}

        {{ form_start(userProfileForm) }}
        {{ form_widget(userProfileForm) }}
        {{ form_end(userProfileForm) }}
        <div style="
            width: 250px;
            height: 250px;
            border-radius: 250px;
            overflow: hidden;">
            <img src="{{ app.user.getAvatar }}" alt="{{ app.user.getAvatar }}" style="
            block-size: inherit;
            ">
        </div>
        {#        <div>{{ app.user.getAvatar }}</div>#}

        {#        {{ form_start(userProfileForm) }}#}
        {#        {{ form_row(registrationForm.username) }}#}
        {#        {{ form_row(registrationForm.email) }}#}
        {#        {{ form_row(registrationForm.plainPassword, {#}
        {#            label: 'Password'#}
        {#        }) }}#}

        {#        <button type="submit" class="btn">Сохранить</button>#}
        {#        {{ form_end(userProfile) }}#}

        {#        <ul>#}
        {#            <li>Your controller at <code><a href="{{ '/var/www/html/src/Controller/UserController.php'|file_link(0) }}">src/Controller/UserController.php</a></code>#}
        {#            </li>#}
        {#            <li>Your template at <code><a href="{{ '/var/www/html/templates/user/index.html.twig'|file_link(0) }}">templates/user/index.html.twig</a></code>#}
        {#            </li>#}
        {#        </ul>#}
    </div>
{% endblock %}
