{{ form_start(form, {'action': path('comment_new', {'id': article.id})}) }}
{#    {{ form_label(form, 'Комментарий', {'label_attr': {'class': 'foo'}}) }}#}
{#    {{ form_widget(form.body, {'attr': {'class': '', 'rows' : '3'}}) }}#}
{#    <div class="">Рейтинг</div>#}
{#    {{ form_widget(form.rating, {'attr': {'class': ''}}) }}#}
{#    <button class="btn">{{ button_label|default('Save') }}</button>#}
{#{{ form_end(form) }}#}
            <div class="form-group row text-center">
                <label for="exampleFormControlTextarea1" class="text-center">Написать комментарий</label>
                {{ form_widget(form.body, {'attr': {'class': 'form-control', 'rows' : '3'}}) }}
{#                <textarea class="form-control" id="exampleFormControlTextarea1" rows="3" name="comment"></textarea>#}
            </div>
            <div class="form-group row">
                <div class="col-sm-10">
                    <button class="btn btn-primary">{{ button_label|default('Отправить') }}</button>
                </div>
            </div>
{{ form_end(form) }}



{#{{ form_start(form,{'action': path('comment_new', {'id': article.id})}) }}#}
{#<div class="my-custom-class-for-errors">#}
{#    {{ form_errors(form) }}#}
{#</div>#}

{#<div class="form-group">#}
{#    <label for="exampleFormControlTextarea1">Написать комментарий</label>#}
{#    {{ form_widget(form.body, {"attr": {"class": "form-control"}}) }}#}
{#</div>#}

{#<button class="btn btn-primary">{{ button_label|default('Save') }}</button>#}
{#{{ form_end(form) }}#}