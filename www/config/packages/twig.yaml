twig:
    default_path: '%kernel.project_dir%/templates'
    globals:
        # the value is the service's id
        navigation: '@App\Service\Page\Navigation'
        user_provider: '@App\Service\User\UserProvider'
        fb_domain: '%app.fb_domain%'
